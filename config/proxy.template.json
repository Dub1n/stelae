{
  "mcpProxy": {
    "baseURL": "{{PUBLIC_BASE_URL}}",
    "addr": ":9090",
    "name": "stelae",
    "version": "1.0.0",
    "type": "streamable-http",
    "options": {
      "panicIfInvalid": true,
      "logEnabled": true,
      "enableManifest": true
    }
  },
  "manifest": {
    "name": "stelae",
    "version": "1.0.0",
    "description": "Local MCP stack",
    "publicBaseURL": "{{PUBLIC_BASE_URL}}",
    "serverName": "stelae",
    "resources": [],
    "toolOverridesPath": "{{TOOL_OVERRIDES_PATH}}",
    "toolSchemaStatusPath": "{{TOOL_SCHEMA_STATUS_PATH}}"
  },
  "mcpServers": {
    "custom": {
      "type": "stdio",
      "command": "{{PYTHON}}",
      "args": [
        "{{STELAE_DIR}}/scripts/custom_tools_server.py"
      ],
      "env": {
        "STELAE_CUSTOM_TOOLS_CONFIG": "{{STELAE_DIR}}/config/custom_tools.json"
      }
    },
    "fs": {
      "type": "stdio",
      "command": "{{FILESYSTEM_BIN}}",
      "args": [
        "{{STELAE_DIR}}"
      ]
    },
    "integrator": {
      "type": "stdio",
      "command": "{{PYTHON}}",
      "args": [
        "{{STELAE_DIR}}/scripts/stelae_integrator_server.py"
      ],
      "env": {
        "STELAE_DISCOVERY_PATH": "{{STELAE_DISCOVERY_PATH}}"
      }
    },
    "rg": {
      "type": "stdio",
      "command": "{{RG_BIN}}",
      "args": [
        "--stdio",
        "--root",
        "{{STELAE_DIR}}"
      ]
    },
    "sh": {
      "type": "stdio",
      "command": "{{SHELL_BIN}}",
      "args": [
        "--workdir",
        "{{PHOENIX_ROOT}}",
        "--allow",
        "npm,pytest,make,python,git"
      ]
    }
  }
}
