# Copy to .env and customise for your workstation.
# Replace the placeholder paths below with the real locations on your machine.
# The integrator automatically appends any MCP-specific variables (e.g. hydrated
# overrides) to your local `.env`, so this template only contains generic defaults.

# Project directories (absolute paths)
STELAE_DIR=/home/your-username/dev/stelae
APPS_DIR=/home/your-username/apps
VENDOR_DIR=${APPS_DIR}/vendor
STELAE_CONFIG_HOME=${HOME}/.config/stelae
STELAE_STATE_HOME=${STELAE_CONFIG_HOME}/.state
STELAE_ENV_FILE=${STELAE_CONFIG_HOME}/.env

# Mutable config/state outputs (required)
STELAE_TOOL_OVERRIDES=${STELAE_CONFIG_HOME}/tool_overrides.json
STELAE_TOOL_AGGREGATIONS=${STELAE_CONFIG_HOME}/tool_aggregations.json
STELAE_CUSTOM_TOOLS_CONFIG=${STELAE_CONFIG_HOME}/custom_tools.json
STELAE_DISCOVERY_PATH=${STELAE_STATE_HOME}/discovered_servers.json
TOOL_OVERRIDES_PATH=${STELAE_STATE_HOME}/tool_overrides.json
TOOL_SCHEMA_STATUS_PATH=${STELAE_STATE_HOME}/tool_schema_status.json
INTENDED_CATALOG_PATH=${STELAE_STATE_HOME}/intended_catalog.json
LIVE_CATALOG_PATH=${STELAE_STATE_HOME}/live_catalog.json
# Catalog defaults (set to 0 to force legacy runtime overrides)
STELAE_USE_INTENDED_CATALOG=1

# Core proxy binaries/config
PROXY_BIN=${APPS_DIR}/mcp-proxy/build/mcp-proxy
PROXY_CONFIG=${STELAE_STATE_HOME}/proxy.json
PROXY_PORT=9090

# Workspace roots
PHOENIX_ROOT=/home/your-username/dev/Phoenix
MEMORY_DIR=${STELAE_CONFIG_HOME}/memory
SEARCH_ROOT=${PHOENIX_ROOT}
SEARCH_PYTHON_BIN=/home/your-username/.venvs/stelae-search/bin/python

# Executable overrides (set to whatever install locations you use)
LOCAL_BIN=/home/your-username/.local/bin
NPX_BIN=npx
FILESYSTEM_BIN=${LOCAL_BIN}/rust-mcp-filesystem
RG_BIN=${LOCAL_BIN}/mcp-grep-server
STRATA_BIN=${LOCAL_BIN}/strata
MEMORY_BIN=${LOCAL_BIN}/basic-memory
PLAYWRIGHT_BROWSERS_PATH=${HOME}/.cache/ms-playwright

# Service tooling
PM2=/home/your-username/.nvm/versions/node/v22.19.0/bin/pm2
ONE_MCP_BIN=${LOCAL_BIN}/uv
ONE_MCP_DIR=${VENDOR_DIR}/1mcpserver
OPENAI_API_KEY=your-openai-key
GITHUB_TOKEN=optional-github-token
# Use the stelae-bridge venv so FastMCP + httpx are available for
# scripts/tool_aggregator_server.py and scripts/stelae_integrator_server.py.
PYTHON=${HOME}/.venvs/stelae-bridge/bin/python
SHIM_PYTHON=${HOME}/.venvs/stelae-bridge/bin/python
CLOUDFLARED=cloudflared
CF_TUNNEL_NAME=stelae

# Ports
PUBLIC_PORT=${PROXY_PORT}

# Public URL (manifest + SSE endpoints)
PUBLIC_BASE_URL=https://your-domain-or-tunnel.example
PUBLIC_SSE_URL=${PUBLIC_BASE_URL}/mcp

# Basic Memory defaults (project name used inside the MCP)
